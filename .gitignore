# Reference implementation (kept locally, not part of the Healix project repo)
TestBot_MCP/

# Dependencies
node_modules/
.pnp/
.pnp.js

# Build output
dist/
out/
.vite/
.turbo/

# Local data / artifacts
*.log
.DS_Store
coverage/
# Playwright's own HTML/JSON reporter output when the real-Playwright
# integration tests (execute.integration.test.ts) are run from a package dir
# directly instead of an os-tmpdir scaffold.
test-results/
# Persisted source-context artifact written into a project's own repoPath by
# target/context-store.ts; also created here when a test uses repoPath: process.cwd().
.healix/

# Secrets / env
.env
.env.*
!.env.example

# packaged app output
apps/desktop/release/

# Local-only design notes (not meant to be committed)
docs/design/prd-excel-csv-upload.md
