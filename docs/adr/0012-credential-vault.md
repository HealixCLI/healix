# ADR-0012: Credential vault / secret storage

- **Status:** Proposed (placeholder)
- **Date:** TBD
- **Deciders:** TBD

## Context

_TODO — how Healix stores target-app login credentials for auth-tier testing: OS keychain (`safeStorage`/`keytar`), per-project scoping, redaction in logs/reports, and guaranteed exclusion from exported suites (carry over the `auth-state-*.json` deny-list)._

## Decision

_TODO_

## Consequences

_TODO_
